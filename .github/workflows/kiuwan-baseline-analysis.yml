name: Kiuwan Baseline Analysis

on:
  push:
    branches: [ master ]

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Test
        run: |
          v1=${{ secrets.KIUWAN_PASS }}
          echo $v1
          echo ${v1%?}
          echo ${v1: -1}
